'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { Package } from 'lucide-react';

export default function BuyerOrdersPage() {
  return (
    <PlaceholderPage
      icon={Package}
      title="My Orders"
      message="Coming in Phase 4 — Track all your orders from placement to delivery."
    />
  );
}
