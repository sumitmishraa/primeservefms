'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { RefreshCw } from 'lucide-react';

export default function BuyerReorderPage() {
  return (
    <PlaceholderPage
      icon={RefreshCw}
      title="Quick Reorder"
      message="Coming in Phase 4 — Reorder your most frequently purchased supplies in one click."
    />
  );
}
