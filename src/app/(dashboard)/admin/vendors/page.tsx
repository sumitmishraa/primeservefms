'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { BookUser } from 'lucide-react';

export default function AdminVendorsPage() {
  return (
    <PlaceholderPage
      icon={BookUser}
      title="Vendor Directory"
      message="Internal contact book for offline vendors — add contacts, phone numbers, and supply categories. This section will be built next."
    />
  );
}
