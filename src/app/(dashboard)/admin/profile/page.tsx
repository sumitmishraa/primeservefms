'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { User } from 'lucide-react';

export default function AdminProfilePage() {
  return (
    <PlaceholderPage
      icon={User}
      title="Profile"
      message="Coming Soon — Manage your admin account details."
    />
  );
}
