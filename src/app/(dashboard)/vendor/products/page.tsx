'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { Package } from 'lucide-react';

export default function VendorProductsPage() {
  return (
    <PlaceholderPage
      icon={Package}
      title="My Products"
      message="Coming in Phase 3 — Add, edit, and manage your housekeeping product catalogue."
    />
  );
}
