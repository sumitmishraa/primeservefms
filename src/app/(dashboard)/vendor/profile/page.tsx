'use client';

import PlaceholderPage from '@/components/ui/PlaceholderPage';
import { User } from 'lucide-react';

export default function VendorProfilePage() {
  return (
    <PlaceholderPage
      icon={User}
      title="Profile"
      message="Coming in Phase 3 — Update your business profile, GST details, and bank account."
    />
  );
}
